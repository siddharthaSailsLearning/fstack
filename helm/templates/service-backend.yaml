apiVersion: v1
kind: Service
metadata:
  name: {{ include "webapp.fullname" . }}-backend
spec:
  selector:
    app: backend
  ports:
  - port: {{ .Values.backend.port }}
    targetPort: {{ .Values.backend.port }}